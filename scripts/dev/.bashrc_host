# 1. Cargamos tu configuración normal de Linux Mint (colores, variables, PATH de Bun)
source ~/.bashrc

# 2. Variables de entorno para acceso desde red local (mint.local)
export VITE_API_URL=http://mint.local:3000
export VITE_WS_URL=ws://mint.local:3001

# 3. Ejecutamos el servidor
bun run dev
